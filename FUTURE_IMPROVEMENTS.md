# Future Improvements for Whispering Lore

## SEO Foundations (deferred)
- Add `WebSite` + `SearchAction` JSON‑LD
- Generate per‑entity `Thing`/`CreativeWork` JSON‑LD for every creature, story and item
- Implement FAQ schema for common questions
- Build a keyword‑volume research pipeline

## Content & Engagement (deferred)
- Weekly “Mythology Spotlight” blog series
- User‑submitted Q&A widget
- Community “Related items” auto‑generated sections
- Social media OG image generation automation

## Performance (deferred)
- Inline critical CSS for hero
- Enable gzip/Brotli on the hosting layer
- Add `Cache‑Control` headers for static assets
- Run PurgeCSS to strip unused selectors

## Accessibility (deferred)
- Conduct full aXe audit and fix any remaining issues
- Add ARIA labels to all decorative SVG icons
- Provide high‑contrast focus outlines for custom widgets

## Analytics & Monitoring (deferred)
- Google Search Console & GA4 integration
- Lighthouse CI pipeline
- Real‑time performance dashboard using `web‑vitals`

## Internationalisation (deferred)
- Add language selection (English + translations)
- Provide `hreflang` tags for each locale

## Structural Improvements (deferred)
- Refactor large JSON data files into paginated API endpoints
- Migrate to a static‑site generator (e.g., Astro, Eleventy) for better templating
- Introduce a build step for image optimization (WebP, AVIF)

These items are kept for future phases; you can prioritize any of them when ready.
